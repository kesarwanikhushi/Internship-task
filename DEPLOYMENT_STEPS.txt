╔══════════════════════════════════════════════════════════════════════╗
║              DEPLOYMENT STEPS - PRODUCTION READY                     ║
╚══════════════════════════════════════════════════════════════════════╝

✅ CODE IS PRODUCTION READY - ALL CORS ISSUES FIXED!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 STEP 1: PREPARE YOUR CODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Ensure all changes are saved
2. Push your code to GitHub:
   
   git add .
   git commit -m "Production ready with CORS fixes"
   git push origin main


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔧 STEP 2: BACKEND DEPLOYMENT (Render.com)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A. Go to https://render.com and sign in

B. Click "New +" → "Web Service"

C. Connect your GitHub repository

D. Configure:
   Name: task-manager-backend (or any name)
   Root Directory: backend
   Environment: Node
   Build Command: npm install
   Start Command: npm start

E. ADD ENVIRONMENT VARIABLES (Click "Advanced" → "Add Environment Variable"):

   MONGODB_URI
   Value: mongodb+srv://kesarwanikhushi121_db_user:TUlqtxu09ZsBo2Z1@cluster0.xy64uqd.mongodb.net/?appName=Cluster0

   JWT_SECRET
   Value: someverysecretvalue

   EMAIL_SERVICE
   Value: gmail

   EMAIL_USER
   Value: kesarwani.khushi121@gmail.com

   EMAIL_PASSWORD
   Value: [YOUR GMAIL APP PASSWORD FROM NODEMAILER - 16 chars, no spaces]

   EMAIL_FROM
   Value: "Task Manager <noreply@taskmanager.com>"

   FRONTEND_URL
   Value: https://internshiptask-lilac.vercel.app

   PORT
   Value: 5000

F. Click "Create Web Service"

G. Wait for deployment (5-10 minutes)

H. **COPY YOUR BACKEND URL** (e.g., https://your-app.onrender.com)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎨 STEP 3: FRONTEND DEPLOYMENT (Vercel)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A. Go to https://vercel.com and sign in

B. Click "Add New..." → "Project"

C. Import your GitHub repository

D. Configure:
   Framework Preset: Vite
   Root Directory: frontend
   Build Command: npm run build
   Output Directory: dist

E. ADD ENVIRONMENT VARIABLE:
   Click "Environment Variables"
   
   Name: VITE_API_URL
   Value: [YOUR BACKEND URL FROM STEP 2H]
   Example: https://your-app.onrender.com

   ⚠️ CRITICAL: NO trailing slash!

F. Click "Deploy"

G. Wait for deployment (2-5 minutes)

H. **COPY YOUR FRONTEND URL** (e.g., https://your-app.vercel.app)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 STEP 4: UPDATE BACKEND CORS (Important!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A. Go back to Render dashboard

B. Click on your backend service

C. Go to "Environment" tab

D. UPDATE the FRONTEND_URL to match your Vercel URL:
   
   FRONTEND_URL
   Value: [YOUR FRONTEND URL FROM STEP 3H]
   Example: https://your-app.vercel.app
   
   ⚠️ CRITICAL: NO trailing slash!

E. Save changes (this will redeploy your backend automatically)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ STEP 5: TEST YOUR DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Open your frontend URL in browser

2. Open Browser DevTools (F12) → Console tab

3. Click "Register"

4. Fill in the form with your email

5. Submit registration

6. ✅ Check Console - Should see NO CORS errors!
   ✅ Should see successful API calls
   ✅ Should see "Registration successful" message

7. Check your email for OTP (check spam folder too)

8. Enter the 6-digit OTP

9. ✅ Should see "Email verified successfully"

10. Click "Login"

11. Enter credentials

12. ✅ Should login successfully and see dashboard


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🐛 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ CORS Error in Console:
   → Check FRONTEND_URL in backend env vars matches your Vercel URL
   → Check VITE_API_URL in frontend env vars matches your Render URL
   → Make sure NO trailing slashes in URLs
   → Redeploy backend after changing env vars

❌ Email Not Arriving:
   → Check EMAIL_PASSWORD is your Gmail App Password (16 chars, no spaces)
   → Check 2FA is enabled on Gmail account
   → Check spam folder
   → Check backend logs in Render dashboard for email errors

❌ Registration Succeeds But No Email:
   → This is OK! Email sends in background
   → Check backend logs for email errors
   → If email service fails, user can click "Resend OTP"

❌ 500 Internal Server Error:
   → Check backend logs in Render dashboard
   → Verify MongoDB connection string is correct
   → Verify all environment variables are set


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 ENVIRONMENT VARIABLES CHECKLIST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Backend (Render):
✅ MONGODB_URI
✅ JWT_SECRET
✅ EMAIL_SERVICE
✅ EMAIL_USER
✅ EMAIL_PASSWORD (Gmail App Password)
✅ EMAIL_FROM
✅ FRONTEND_URL (your Vercel URL, no trailing slash)
✅ PORT

Frontend (Vercel):
✅ VITE_API_URL (your Render URL, no trailing slash)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 DONE!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Your app is now live and production-ready!

Share your URLs:
Frontend: https://your-app.vercel.app
Backend:  https://your-app.onrender.com

All CORS issues are FIXED! ✨
