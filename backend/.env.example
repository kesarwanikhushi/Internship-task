# Server Configuration
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=your_mongodb_connection_string_here
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/taskmanager

# JWT Secret (generate a strong random string)
JWT_SECRET=your_jwt_secret_key_here

# Email Configuration
# Option 1: Gmail (recommended for production)


# Option 2: Custom SMTP (e.g., SendGrid, Mailgun)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key

# Option 3: For development/testing (Ethereal - fake SMTP)
# Leave EMAIL_SERVICE empty to use Ethereal
# Create free account at https://ethereal.email
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_USER=your-ethereal-user
# SMTP_PASS=your-ethereal-password

# Frontend URL (for password reset links, CORS, etc.)
FRONTEND_URL=http://localhost:5173

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
